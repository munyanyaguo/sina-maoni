{
  "name": "@sina-maoni/desktop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "lint": "eslint .",
    "test": "vitest",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vitest": "^2.0.0"
  }
}
