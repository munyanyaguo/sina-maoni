{
  "name": "@sina-maoni/extension",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wxt dev",
    "build": "wxt build",
    "lint": "eslint .",
    "test": "vitest",
    "type-check": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "wxt": "^0.18.0",
    "vitest": "^2.0.0"
  }
}
