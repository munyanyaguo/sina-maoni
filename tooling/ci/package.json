{
  "name": "@sina-maoni/ci",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "bin": {
    "sina-maoni": "dist/cli.mjs"
  },
  "main": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest",
    "type-check": "tsc -p tsconfig.json --noEmit",
    "publish:dry": "npm publish --dry-run"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
